server {
    listen 80;
    server_name mcp.talkwithme.r-e.kr;

    location ^~ /.well-known/acme-challenge/ {
        root /var/lib/letsencrypt;
        default_type text/plain;
    }

    location / {
        return 404;
    }
}
