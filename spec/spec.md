# Feature: DIEM V2 경제·시사 릴스 발행 파이프라인

## Outcome

기존 오늘경제 파이프라인을 `DIEM — Daily Issue & Economy Magazine`으로
리브랜딩하고, 매일 한국시간 기준 경제 뉴스 1건과 시사 뉴스 1건을
선정해 각각 7초 릴스로 자동 발행한다.

독자는 원문을 모두 읽지 않아도 정확히 3문장 안에서 그날의 핵심 사실과
기사에 명시된 맥락을 이해할 수 있어야 한다. 운영자는 별도 데이터베이스나
상시 유료 인프라 없이 GitHub 기록만으로 선정 근거, 생성물, 권리 정보,
발행 상태와 실패 복구 과정을 추적할 수 있어야 한다.

## User and context

- 핵심 독자는 경제·시사 흐름을 챙길 시간이 부족한 20~30대 직장인,
  사회초년생, 재테크 초보자다.
- 운영자는 GitHub Actions와 Instagram 전문 계정을 이용해 하루 두 번의
  발행 상태를 확인하고, 자동 복구가 끝난 예외만 직접 처리한다.
- 본 스펙은 기존 `@today.econ` 계정을 유지한 채 DIEM으로 전환하는
  V2 파이프라인을 정의한다.

## Required behavior

### 0. 채널 리브랜딩

1. 브랜드 표기는 `DIEM — Daily Issue & Economy Magazine`으로 통일한다.
   `DIEM`에는 “오늘을 놓치지 않는다”는 `carpe diem`의 연상을 담되,
   타 브랜드의 이름·로고·문구를 복제하지 않는다.
2. 기존 Instagram 계정의 우선 사용자 이름은 `@diem.magazine`, 실제
   변경 화면에서 사용할 수 없으면 `@diem_magazine`으로 한다.
3. 소개 문구는 다음 세 줄을 사용한다.

   ```text
   오늘을 놓치지 않는 경제·시사 브리핑
   매일 경제 1편 · 시사 1편
   20·30 재테크 초보자를 위한 1분 매거진
   ```

4. 브랜드 해시태그는 `#diem`, `#diemmagazine`,
   `#데일리이슈앤이코노미`를 사용한다. 사용자 이름에 점이 포함되더라도
   해시태그에는 점을 사용하지 않는다.
5. 기존 게시물은 삭제·보관·소급 수정하지 않고 유지한다. 별도의 전환
   공지 게시물 없이 V2 발행을 시작한다.
6. Instagram의 이름·사용자 이름·소개 문구 변경은 운영자가 앱에서
   수동으로 수행한다. 파이프라인은 실제 적용된 사용자 이름을 설정값으로
   받아 댓글의 계정 멘션에 사용한다.
7. 프로필 이미지는 다음 조건을 만족하는 정사각형 PNG로 제작한다.
   - 짙은 네이비 배경 중앙에 흰색 `DIEM` 워드마크
   - `I` 또는 짧은 포인트 선에만 DIEM 블루 사용
   - 작은 원형 크롭에서 읽히는 안전 여백
   - 긴 영문 확장명과 기존 today.econ 로고는 제외

### 1. 주제 선정

#### 1.1 수집 범위와 인기 점수

1. 한국시간 기준 해당 날짜에 게시된 뉴스 중 네이버와 다음의 공개
   일간 인기 뉴스 목록을 각각 최대 50위까지 수집한다.
2. 각 포털의 순위는 목록 크기를 `N`, 1부터 시작하는 순위를 `r`이라
   할 때 `100 - 99 × (r - 1) / (N - 1)`로 1~100점에 정규화한다.
   목록이 한 건이면 100점으로 처리한다.
3. URL 정규화, 정규화된 제목, 핵심 개체와 사건 서명을 이용해 동일
   기사 또는 동일 사건을 하나의 후보로 병합한다.
4. 통합 인기 점수는 포털별 정규화 점수의 평균에 양쪽 포털에 모두
   등장한 경우 10점을 더한 값으로 한다. 임의의 AI 편집 점수로 순서를
   재정렬하지 않는다.
5. 한 포털 수집이 실패하면 다른 포털만으로 계속한다. 둘 다 실패하면
   기존 언론사 RSS 후보를 사용하고 `popularity_fallback`과 실패 이유를
   기록한다. 이 경우 “전국 최다 조회”처럼 검증할 수 없는 표현을 쓰지
   않는다.

#### 1.2 분야 규칙

1. 경제 분야에는 금리, 물가, 환율, 세금, 부동산, 대출, 예·적금,
   금융시장, 고용, 소득과 돈·일자리에 실질적인 영향을 주는 반도체,
   AI, 자동차, 유통 등 주요 산업 변화를 포함한다.
2. 단순 종목 추천, 근거 없는 전망, 일시적인 가격 등락, 일반 인사,
   단순 MOU와 홍보성 제품 기사는 경제 후보에서 제외한다.
3. 시사 분야에는 정책, 노동·고용, 주거, 교육, 인구, 복지, 사회 변화,
   국제 정세와 기술 규제를 포함한다.
4. 정치는 정책이나 생활 영향이 분명할 때만 포함한다. 정당 간 공방,
   자극적인 범죄·사고, 연예, 스포츠와 가십은 제외한다.
5. 한 후보가 두 분야에 모두 해당하면 핵심 사건의 직접 영향이 더 큰
   한 분야에만 배정한다. 같은 사건을 하루 두 편에 중복 사용하지 않는다.

#### 1.3 7일 중복 방지

1. 모든 후보에 대해 다음 항목으로 간결한 `topic_signature`를 만든다.
   - 분야
   - 핵심 대상
   - 사건 또는 변화
   - 주요 인물·기업·정책명
2. 로컬 다국어 문장 임베딩으로 최근 7일간 모든 분야의 발행 기록과
   코사인 유사도를 비교한다.
3. 기본 임베딩 모델은
   `sentence-transformers/paraphrase-multilingual-MiniLM-L12-v2`로
   하고 GitHub Actions 캐시를 사용한다.
4. 판정 기준은 다음과 같다.
   - `0.78 이상`: 자동 중복
   - `0.68 이상 0.78 미만`: 핵심 대상과 사건이 모두 겹칠 때 중복
   - `0.68 미만`: 새 주제
5. 모델 로딩 또는 추론 실패 시 정규화된 제목, 핵심 개체와 사건
   키워드의 결정적 중복 점수로 폴백하고 그 사실을 기록한다.
6. 같은 주제라도 새로운 최종 결정, 확정 수치, 판결, 금리 결정처럼
   결과를 실질적으로 바꾸는 후속 보도는 예외적으로 허용한다.
   `repeat_override: true`와 구체적인 이유가 필수이며 단순 논평이나
   추측은 예외로 인정하지 않는다.
7. 실제 자동 발행 전에 중복·비중복 한국어 기사 쌍 30~50개로 회귀
   데이터셋을 만들고 위 임계값의 기대 판정을 고정한다.

#### 1.4 선정과 사실 검증

1. 통합 인기 순서대로 분야 적합성, 7일 중복, 원문 접근성, 게시 시각,
   출처, 이미지 확보 가능성을 검사하고 각 분야에서 가장 먼저 모든
   조건을 통과한 한 건을 선택한다.
2. 선정 원문을 기본 근거로 사용하되 핵심 수치, 인물, 기업, 정책명과
   시행일은 공식 자료 또는 별도의 신뢰 가능한 기사 한 건으로 교차
   확인한다.
3. 교차 확인 자료는 다른 도메인이며 단순 재전재가 아닌 독립 출처여야
   한다. 공식 발표 자료가 있으면 이를 우선한다.
4. 근거가 충돌하거나 핵심 사실을 확인할 수 없으면 다음 순위 후보로
   넘어간다.
5. 해당 날짜의 모든 후보가 품질 기준을 통과하지 못하면 품질이 낮은
   기사나 전날 기사로 채우지 않는다. 해당 분야를 `no_publish`로
   기록하고 다른 분야는 독립적으로 계속한다.

### 2. 타이틀과 표지

1. 타이틀은 공백을 포함한 가시적 grapheme 기준 총 14자 이하며 정확히
   두 줄이다. 줄바꿈 문자는 글자 수에서 제외하고 각 줄은 4~7자를
   권장한다.
2. 첫 줄은 DIEM 블루, 둘째 줄은 화이트로 표시한다.
3. 생성 모델은 기사 근거 안에서 후보 5개를 만들고 다음 우선순위로
   하나를 선택한다.
   - 사실 정확성
   - 길이와 2줄 형식
   - 즉시 이해되는 핵심 사건
   - 원문을 읽고 싶게 하는 호기심
4. 핵심 사실을 숨기는 “모르면 손해”, “결국 터졌다”, “대체 왜?” 같은
   낚시 표현, 불필요한 따옴표·문장부호와 사실 왜곡은 금지한다.
5. 검증된 수치가 사건의 핵심이면 수치를 사용할 수 있으나, 숫자만
   강조해 맥락을 왜곡해서는 안 된다.
6. 표지 메타는 한국시간 발행일 기준으로 정확히 다음 중 하나를 쓴다.
   - `YYYY.MM.DD · ECONOMY`
   - `YYYY.MM.DD · ISSUE`
7. 표지는 1080×1920이며 단일 DIEM 디자인 시스템을 사용한다.
   - 배경: `#080C16`
   - 블루: `#4D7CFE`
   - 화이트: `#F7F9FC`
   - 보조색: `#A6AEC1`
   - 서체: Pretendard
   - 타이틀: ExtraBold와 좁은 자간
   - 날짜·분야: Medium과 넓은 자간
8. 사진 위에 어두운 그라데이션을 적용하고 텍스트 안전 영역을 확보한다.
   기존 Obsidian·Ivory·Cyber 테마는 V2 표지에서 사용하지 않는다.

### 3. 웹 이미지 선정

1. 이미지 출처 허용 목록은 다음과 같다.
   - Pexels 우선
   - Unsplash 차순위
   - 라이선스가 기계 판독 가능하며 Public Domain, CC0, CC BY 또는
     프로젝트와 양립 가능한 조건임을 확인한 Wikimedia Commons
   - 재사용 허가가 명시된 공공기관·정부·기업 보도 자료
2. 일반 검색 결과, 뉴스 기사에 삽입된 사진, 워터마크가 있는 이미지와
   재사용 권한을 확인할 수 없는 이미지는 사용하지 않는다.
3. 핵심 대상, 사건과 분야로 3~5개의 검색어를 만들고 최대 20개 후보를
   수집한다.
4. 라이선스, 최소 해상도, 세로 크롭 가능성, 깨진 URL, 워터마크를 먼저
   검사한 뒤 다음 요소로 점수를 계산한다.
   - 주제·타이틀과의 의미 일치도
   - 9:16 크롭에서 핵심 피사체와 텍스트 안전 영역 보존
   - 타이틀 가독성을 해치지 않는 시각적 단순성
5. 선택 신뢰도가 기준 미만이면 다음 순서로 폴백한다.
   - 핵심 대상의 직접 사진
   - 사건을 설명하는 개념 사진
   - 최근 7일 동안 쓰지 않은 사전 검수 분야 이미지
   - 사진 없이 DIEM 타이포그래피만 사용하는 표지
6. 원본 URL, 제작자, 서비스, 라이선스, 검색어, 조회 시각, 점수와 선택
   이유를 발행 기록에 남긴다.

### 4. 본문·댓글·해시태그

#### 4.1 본문

1. NFC 정규화 후 정확히 3개의 한국어 문장으로 구성한다.
2. 각 문장은 공백, 문장부호와 이모지를 포함한 가시적 grapheme 기준
   최대 120자이며 문장 내부 줄바꿈은 없다.
3. 문장 사이는 빈 줄 하나, 즉 `\n\n`으로 구분한다. URL과 해시태그는
   본문에 넣지 않는다.
4. 문장 역할은 다음과 같다.
   - 1문장: 무슨 일이 일어났는지
   - 2문장: 핵심 수치·사실·맥락
   - 3문장: 기사나 교차 검증 근거에 명시된 배경·전망·의미. 명시된
     내용이 없으면 다음으로 중요한 사실
5. 기사에 없는 투자 조언, 생활 영향, 전망이나 인사이트를 만들지 않는다.
6. 1문장 끝에는 내용에 맞는 이모지 정확히 1개, 2문장에는 이모지 0개,
   3문장 끝에는 내용에 맞는 이모지 정확히 1개를 둔다.
7. 두 이모지는 달라도 되며 같은 이모지가 가장 적합할 때만 중복한다.
   사실이나 감정을 과장하는 이모지는 금지하고, 정치·재난·참사에는
   가벼운 이모지를 사용하지 않는다. 폴백은 경제 `📊`, 시사 `📰`다.
   OBMC를 연상시키는 `🍏`을 기본값으로 사용하지 않는다.

#### 4.2 댓글 체인

1. 릴스 발행 후 첫 댓글은 1문장 끝에 사용한 이모지 하나만 포함한다.
2. 첫 댓글의 대댓글은 실제 계정 사용자 이름 멘션과 12~15개 해시태그로
   구성하며 중복, 점과 공백이 포함된 해시태그를 금지한다.
3. 해시태그 구성은 총 15개 이내에서 다음 범위를 따른다.
   - 분야 기본 태그 3개
   - 독자·형식 태그 2~3개
   - DIEM 브랜드 태그 3개
   - 기사에 실제 등장하는 주제·기업·인물 태그 4~6개
4. 태그 수 때문에 Instagram API가 거절한 것이 확인되면 핵심 태그
   5개로 한 번만 재시도한다. 원래 목록과 축소 목록을 모두 기록한다.
5. 릴스, 첫 댓글과 대댓글은 독립된 상태로 관리한다. 댓글 실패 때문에
   이미 발행된 릴스를 삭제하거나 다시 올리지 않는다.
6. 댓글 단계는 같은 Instagram 미디어·댓글 ID에만 최대 3회 재시도한다.
   성공한 단계는 다시 실행하지 않으며 3회 실패 후
   `manual_action_required`로 전환한다.
7. 구현 전 Instagram 전문 계정과 액세스 토큰에 댓글 관리 권한이
   있는지 확인한다. 권한이 없으면 릴스 발행은 계속하고 댓글 단계만
   `manual_action_required`로 기록한다.

### 5. 릴스·음악·발행

#### 5.1 릴스 구성

1. 릴스 한 편은 표지 이미지 한 장으로 만든 1080×1920, 7초 영상이다.
2. 영상에는 날짜·분야와 두 줄 타이틀만 표시하고 3문장 본문, 추가
   슬라이드, 차트와 액션 카드를 넣지 않는다.
3. 사진에는 반복 재생 경계가 튀지 않는 미세한 줌 또는 팬을 적용한다.
4. 출력은 Instagram 수집에 적합한 H.264, 30fps, `yuv420p`, fast-start
   MP4와 AAC 오디오를 사용한다.

#### 5.2 음악

1. 초기 V2에는 직접 제작한 DIEM 오리지널 무보컬 음원 6곡을 둔다.
   - 경제용: 안정적인 비트, 밝은 미니멀, 테크 펄스 각 1곡
   - 시사용: 중립 뉴스룸, 차분한 다큐멘터리, 절제된 리듬 각 1곡
2. 음원은 7초 루프에 맞추고 낮은 음량과 짧은 시작·종료 페이드를
   적용한다.
3. 같은 분야의 직전 사용곡은 제외하고 최근 사용 횟수가 가장 적으며
   주제 분위기에 맞는 곡을 선택한다.
4. 재난·참사 등 음악이 부적절한 주제는 무음으로 발행한다.
5. 곡 ID, 분류, 선택 이유, 라이선스 선언과 SHA-256을 GitHub에 기록한다.
6. 선택 음원 합성 실패 시 같은 분야의 다른 곡으로 한 번 재시도하고,
   다시 실패하면 릴스 발행을 막지 않고 무음으로 출력한다.
7. 기존의 단순 3음 사인파, Instagram 유행 음원의 API 자동 선택과
   라이선스가 불명확한 외부 음원은 사용하지 않는다.

#### 5.3 일정과 독립 실행

1. 한국시간 18:30 실행에서 당일 경제·시사 후보를 함께 선정하고
   확정된 일일 큐를 GitHub에 먼저 기록한다.
2. 같은 실행에서 경제 릴스를 발행한다.
3. 한국시간 21:00 실행에서는 같은 일일 큐를 읽어 시사 릴스를
   발행한다. 밤 실행에서 임의로 다른 기사를 다시 선정하지 않는다.
4. 두 분야와 릴스·댓글 단계는 독립 상태와 재시도를 가진다. 경제
   실패가 시사 발행을 막거나 그 반대가 되어서는 안 된다.
5. 수동 실행은 기본적으로 누락되거나 미완료인 단계만 처리한다.
6. 릴스 생성·업로드는 재시도하되 Carousel이나 Story로 폴백하지 않는다.
7. 각 건은 날짜·분야로 결정되는 안정적인 `publication_key`를 가지며
   발행 전 GitHub 상태와 Instagram의 최근 게시물을 조정해 중복 발행을
   방지한다.

### 6. GitHub 기록과 상태 모델

1. 별도 데이터베이스 없이 다음 자료를 Git에 영구 보관한다.
   - 일일 후보와 순위·탈락 이유
   - 선정 기사와 교차 검증 URL·확인 시각
   - 주제 서명·유사도·중복 판정과 예외 사유
   - 제목 후보와 최종 선택 이유
   - 본문·이모지·댓글·해시태그
   - 이미지 출처·라이선스·점수
   - 음원 ID·권리 정보·파일 해시
   - 단계별 상태·시도 횟수·오류·Instagram ID와 링크
2. 일일 원장은 `data/publications/YYYY/MM/YYYY-MM-DD.json`에 경제와
   시사 두 항목을 함께 저장한다.
3. 7일 중복과 음원 순환을 위한 작은 파생 인덱스는
   `data/editorial-history.json`에 저장한다. 원장은 진실의 원본이고
   인덱스는 원장에서 다시 만들 수 있어야 한다.
4. 상태는 최소 `planned`, `generating`, `ready`, `publishing`,
   `published`, `no_publish`, `retry_pending`, `failed`,
   `manual_action_required`를 사용한다. 릴스, 첫 댓글과 대댓글은 각각
   자신의 상태·시도 횟수·외부 ID를 가진다.
5. 같은 브랜치의 발행 작업은 GitHub Actions concurrency group으로
   직렬화하고 진행 중 작업을 취소하지 않는다. 상태 커밋 전에는 원격
   변경을 다시 읽고 충돌을 조정한다.
6. 발행 전 `ready` 상태를 커밋하고 발행 후 외부 ID와 결과를 다시
   커밋한다. 발행 후 커밋이 실패한 경우 다음 실행은 최근 Instagram
   게시물의 날짜·분야·정확한 본문을 대조해 기존 게시물을 복구한 뒤
   새 게시 여부를 결정한다.
7. 원본 이미지와 최종 MP4는 Instagram 수집을 위해 GitHub 임시
   Release에 올리고 최대 72시간 뒤 삭제한다. 대용량 미디어를 Git
   이력에 영구 누적하지 않는다.
8. 임시 미디어가 삭제돼도 모든 텍스트, 출처, 권리 정보, 선택 점수,
   외부 게시물 ID와 최종 파일 해시는 영구 기록한다.

### 7. 생성 모델과 폴백

1. Groq 무료 티어를 유지하며 기본 생성 모델은
   `openai/gpt-oss-120b`, 폴백은 `qwen/qwen3.6-27b`로 한다.
2. 모델 식별자는 중앙 설정값으로 관리하고 응답 기록에는 실제 사용
   모델을 남긴다.
3. LLM은 제목 후보와 기사 근거 기반 3문장 초안에만 사용한다. 인기
   순위, 중복 임계값, 길이, 줄 수, 이모지와 해시태그 형식은 코드가
   결정적으로 검증한다.
4. 429 응답에서는 `retry-after`를 우선 사용한다. 무료 한도 초과를
   유료 호출로 자동 전환하지 않는다.
5. 모든 생성 모델 호출이 실패하면 검증된 기사 근거 문장에서
   결정적으로 3문장을 조립하고 규칙 기반 타이틀 후보를 사용한다.
   폴백 결과도 동일한 형식·사실 검증을 통과해야 한다.

### 8. 알림과 운영

1. GitHub의 일일 원장, 커밋과 Issue를 상태의 원본으로 사용한다.
2. Slack은 콘텐츠 전달이 아닌 사람에게 필요한 상태 변화 알림에만
   사용한다.
3. Slack 알림은 다음 이벤트만 보낸다.
   - 발행 완료: 분야, 제목과 Instagram 링크
   - 최초 실패와 재시도 예정: 실패 단계, 시도 횟수와 다음 처리
   - 재시도 복구 성공
   - 최종 실패와 수동 조치 필요: GitHub Issue·Actions 링크
   - 적합한 후보가 없어 발행하지 않음: 분야와 주요 탈락 사유
4. 짧은 자동 재시도마다 중복 알림을 보내지 않고 상태가 바뀔 때만
   보낸다. 본문 전문, 이미지와 영상 파일은 Slack으로 전송하지 않는다.
5. `manual_action_required`와 `no_publish`는 중복 방지 키를 가진 GitHub
   Issue를 생성한다. 같은 발행 건의 반복 오류는 기존 Issue에 누적하고
   복구되면 자동으로 닫는다.

## Acceptance criteria

- [ ] **AC-01 브랜드 자산:** DIEM 이름, 소개 문구, 실제 사용자 이름
  설정과 원형 크롭 가능한 프로필 PNG가 정의된 브랜드 규칙과 일치한다.
- [ ] **AC-02 일일 후보:** 한국시간 당일 네이버·다음 인기 뉴스가 각각
  최대 50건 수집되고, 통합 점수·분야·탈락 이유가 일일 원장에 남는다.
- [ ] **AC-03 포털 폴백:** 한 포털 실패 시 다른 포털로 계속하고 양쪽
  실패 시 RSS 폴백과 `popularity_fallback`을 기록한다.
- [ ] **AC-04 분야 분리:** 경제 1건과 시사 1건이 서로 다른 사건이며
  포함·제외 규칙을 통과한다.
- [ ] **AC-05 중복 판정:** 7일 전체 기록에 대해 0.78/0.68 임계값,
  회색 구간 규칙, 의미 있는 후속 보도 예외와 결정적 폴백이 테스트된다.
- [ ] **AC-06 사실 검증:** 모든 발행 건은 원문과 독립 교차 검증 근거를
  가지며 핵심 사실이 충돌한 후보는 발행되지 않는다.
- [ ] **AC-07 품질 우선:** 적합한 후보가 없는 분야는 다른 날짜나 낮은
  품질의 기사로 채우지 않고 `no_publish`와 알림을 남긴다.
- [ ] **AC-08 타이틀:** 최종 타이틀은 공백 포함 14자 이하, 정확히
  두 줄이며 첫 줄 블루·둘째 줄 화이트이고 낚시 금지 규칙을 통과한다.
- [ ] **AC-09 이미지 권리:** 선택 이미지에는 허용 출처, 제작자,
  라이선스, 원본 URL, 조회 시각, 점수와 선택 이유가 기록된다.
- [ ] **AC-10 이미지 폴백:** 직접 사진부터 무사진 타이포그래피까지의
  폴백이 깨진 URL·낮은 신뢰도에서도 발행 가능한 표지를 만든다.
- [ ] **AC-11 본문 형식:** 본문은 NFC 기준 정확히 3문장, 문장당 120
  grapheme 이하, 문장 사이 빈 줄 하나이며 URL·해시태그가 없다.
- [ ] **AC-12 이모지:** 1·3문장 끝에만 관련 이모지 한 개씩 있고
  2문장에는 이모지가 없으며 민감한 주제의 안전 규칙을 통과한다.
- [ ] **AC-13 댓글 체인:** 첫 댓글은 1문장의 이모지 하나뿐이고
  대댓글은 실제 계정 멘션과 중복 없는 12~15개 해시태그로 구성된다.
- [ ] **AC-14 댓글 독립 복구:** 댓글 권한 또는 API 오류가 릴스 성공
  상태를 되돌리지 않으며, 성공 단계 재실행 없이 최대 3회 복구한다.
- [ ] **AC-15 릴스 출력:** 각 게시물은 1080×1920, 7초, 단일 표지,
  미세한 반복 동작, H.264/AAC 조건을 만족하며 추가 카드가 없다.
- [ ] **AC-16 음악 순환:** 경제 3곡·시사 3곡이 최근 사용 이력과 주제
  안전성에 따라 순환하고 합성 실패 또는 민감 주제는 무음 폴백한다.
- [ ] **AC-17 발행 일정:** 18:30 KST에 같은 날 두 분야를 확정해 경제를
  발행하고, 21:00 KST에는 같은 큐에서 시사를 발행한다.
- [ ] **AC-18 독립·멱등 실행:** 한 분야 또는 댓글 실패가 다른 단계를
  막지 않으며 수동 실행은 미완료 단계만 처리하고 동일
  `publication_key`의 중복 릴스를 만들지 않는다.
- [ ] **AC-19 GitHub 원장:** 일일 원장과 재생성 가능한 인덱스에 모든
  근거·생성·권리·상태·외부 ID가 남고 미디어 바이너리는 영구 누적되지
  않는다.
- [ ] **AC-20 무료 모델 폴백:** 새 Groq 기본·폴백 모델과 규칙 기반
  최종 폴백이 동일한 출력 검증을 통과하며 유료 호출로 전환하지 않는다.
- [ ] **AC-21 운영 알림:** GitHub Issue와 Slack이 정의된 상태 변화만
  중복 없이 알리고 Slack에 콘텐츠 파일이나 본문 전문을 보내지 않는다.
- [ ] **AC-22 V2 전용 발행:** V2 자동 발행은 Reel만 만들며 Carousel과
  Story를 만들거나 폴백으로 사용하지 않는다.

## Constraints

- 모든 한글 텍스트 입력과 저장 전에 NFC 정규화를 적용한다.
- 별도 데이터베이스, 상시 서버와 상시 유료 인프라를 도입하지 않는다.
- Groq 무료 티어, GitHub Actions·커밋·Issues·임시 Releases와 기존
  Instagram·Slack 연결을 사용한다.
- 새 런타임 의존성은 로컬 다국어 임베딩과 이미지 의미 일치도 검증에
  필요한 최소 범위로 제한하며 버전과 모델 ID를 고정하고 캐시한다.
- 외부 이미지·음원은 상업적 사용과 필요한 재배포 권리가 확인된 경우만
  사용한다. 권리가 불명확하면 무사진 또는 무음으로 폴백한다.
- Instagram은 전문 계정이어야 하며 콘텐츠 발행과 댓글 관리 권한을
  구분해 사전 점검한다.
- 예약 실행 지연이 발생해도 일일 기준일과 표지 날짜는 Asia/Seoul
  시간대로 계산한다.
- 외부 포털 HTML과 Meta·Groq API는 변경될 수 있으므로 파서 계약,
  상태 코드와 폴백 경로를 테스트 가능하게 격리한다.
- 기존 사용자 변경사항과 V1 데이터는 덮어쓰지 않는다.

## Verification

### Acceptance criteria mapping

| 기준 | 자동 검증 | 수동 검증 |
| --- | --- | --- |
| AC-01 | 프로필 PNG 크기·안전 영역·색상 스냅샷 테스트 | 실제 계정 이름·소개·원형 크롭 확인 |
| AC-02~04 | 고정된 포털 fixture로 순위 정규화·병합·분류·탈락 이유 테스트 | 샘플 날짜 후보 10건의 분야와 인기 순서 검토 |
| AC-05 | 30~50개 라벨 기사 쌍 회귀 테스트와 모델 실패 폴백 테스트 | 회색 구간·후속 보도 예외 표본 검토 |
| AC-06~07 | 교차 검증 성공·충돌·미확인·후보 소진 fixture 테스트 | 실제 기사 2건의 근거 URL과 문장 대조 |
| AC-08 | grapheme·2줄·금지 표현·색상 렌더 테스트 | 경제·시사 표지 각 3개 가독성 검토 |
| AC-09~10 | 라이선스 허용 목록·URL·점수·4단계 폴백 테스트 | 출처 링크와 9:16 크롭 표본 검토 |
| AC-11~13 | NFC·문장 수·120자·개행·이모지·태그 생성 속성 테스트 | 실제 본문·댓글 체인 2건 복사 검토 |
| AC-14 | 릴스 성공 후 댓글 권한 오류·재시도·중복 방지 통합 테스트 | Meta 테스트 미디어에서 댓글·대댓글 확인 |
| AC-15 | `ffprobe`로 해상도·길이·코덱·fps·오디오 검사 | 7초 루프 경계와 모바일 가독성 확인 |
| AC-16 | 6곡 순환·직전곡 제외·민감 주제·합성 실패 테스트 | 경제·시사 3곡씩 음량·분위기 청취 |
| AC-17~18 | KST 시계 fixture, 같은 큐 재사용, 부분 실패·수동 재실행·조정 테스트 | GitHub Actions 수동 dry-run과 2회 예약 모의 실행 |
| AC-19 | 원장 JSON 스키마·인덱스 재생성·72시간 정리 테스트 | Git 이력과 임시 Release 정리 결과 확인 |
| AC-20 | 기본·폴백·429·전체 실패 mock과 출력 검증 테스트 | Groq 무료 계정으로 경제·시사 샘플 생성 |
| AC-21 | 상태 변화 dedupe·Issue 생명주기·Slack payload 테스트 | Slack 메시지와 GitHub Issue 링크 확인 |
| AC-22 | V2 발행 경로가 Reel 외 API를 호출하지 않는 회귀 테스트 | Instagram 테스트 발행 형식 확인 |

### Project checks

- 자동: `node .codex-harness/scripts/verify-project.mjs`
  - `npm run test`
  - `git diff --check`
- 구현 단계에서 추가:
  - 단위 테스트: 수집·점수·분류·중복·문장·타이틀·태그·음악 선택
  - 통합 테스트: 일일 원장 상태 전이, 두 예약 실행, 부분 실패 복구,
    Instagram·GitHub·Slack API mock
  - 정적 검사: 저장 JSON 스키마와 NFC 검사
  - 미디어 검사: `ffprobe`와 렌더 스냅샷
- 수동:
  - 실제 계정 변경 전 dry-run 결과 검토
  - 라이선스 기록과 기사 근거 대조
  - 프로필·표지 원형/9:16 시각 확인
  - 테스트 Reel 1건과 댓글 체인 확인
  - 경제·시사 예약 실행을 하루 동안 관찰

## External references

- 네이버 뉴스 검색 API는 조회수·인기 순위 필드를 제공하지 않으므로
  검색 API 결과를 “가장 많이 본 뉴스”로 간주하지 않는다:
  [네이버 뉴스 검색 API](https://developers.naver.com/docs/serviceapi/search/news/news.md)
- 중복 판정 모델과 코사인 유사도 사용법:
  [Multilingual MiniLM model](https://huggingface.co/sentence-transformers/paraphrase-multilingual-MiniLM-L12-v2),
  [Sentence Transformers semantic similarity](https://sbert.net/docs/sentence_transformer/usage/semantic_textual_similarity.html)
- 이미지 사용 조건:
  [Pexels License](https://www.pexels.com/license/),
  [Unsplash License](https://unsplash.com/license),
  [Wikimedia Commons reuse](https://commons.wikimedia.org/wiki/Commons:Reusing_content_outside_Wikimedia)
- Instagram 전문 계정의 발행·댓글 권한:
  [Meta Instagram API collection](https://www.postman.com/meta/instagram/documentation/6yqw8pt/instagram-api)
- 상업적 음악 사용과 권리 책임:
  [Instagram music guidance](https://www.facebook.com/help/instagram/402084904469945),
  [Meta Music Guidelines](https://www.facebook.com/legal/music_guidelines)
- 기존 Groq 모델의 2026-08-16 종료와 대체 모델:
  [Groq deprecations](https://console.groq.com/docs/deprecations),
  [Groq free-plan rate limits](https://console.groq.com/docs/rate-limits)

## Out of scope

- 새 Instagram 계정 생성과 기존 게시물 삭제·보관·소급 수정
- 별도의 DIEM 전환 공지 게시물
- Carousel, Story, 다중 카드, 음성 내레이션과 영상 내 3문장 본문
- Instagram 유행 음원 또는 라이선스가 불명확한 외부 음원의 자동 선택
- 경제·시사 외 분야, 하루 2편을 넘는 자동 발행과 실시간 속보
- 개인별 투자 추천, 매수·매도 신호와 기사에 없는 자체 인사이트
- 유료 LLM·이미지·음원 서비스와 별도 DB·상시 서버
- Slack으로 본문·이미지·영상 콘텐츠 전달
- Slack 외 다른 메신저 연동
- 기존 분석 데이터의 전면 마이그레이션이나 성과 기반 자동 최적화
- OBMC의 이름·로고·문구·디자인 복제

## Risks and rollback

- **포털 구조 변경:** 네이버·다음 HTML 변경으로 인기 목록 수집이
  중단될 수 있다. 포털별 어댑터와 fixture를 격리하고 한쪽 포털 또는
  RSS로 폴백한다.
- **기사·이미지 권리:** 원문 접근 제한이나 이미지 라이선스 변경이
  발생할 수 있다. 권리·근거를 실행 시점에 기록하고 불명확하면 후보
  제외 또는 무사진 표지를 사용한다.
- **모델·무료 한도:** Groq 모델 또는 무료 한도가 변경될 수 있다.
  중앙 모델 설정과 규칙 기반 폴백으로 발행을 유지하며 유료 전환은
  별도 결정 없이는 금지한다.
- **Meta 권한:** 댓글 권한 미승인이나 토큰 만료가 발생할 수 있다.
  릴스와 댓글 상태를 분리하고 GitHub Issue·Slack으로 수동 조치를
  알린다.
- **외부 발행 후 상태 커밋 실패:** Instagram 발행 직후 Git 커밋이
  실패하면 중복 위험이 남는다. 재실행 전에 최근 미디어의 날짜·분야와
  정확한 본문을 대조하며 불확실하면 자동 재발행하지 않고 수동 조치로
  전환한다.
- **Git 충돌:** 수동·예약 실행이 겹칠 수 있다. concurrency group과
  원격 상태 재조회로 직렬화하고 서로 다른 분야의 상태를 덮어쓰지 않는다.
- **계정명 롤백:** Instagram 사용자 이름 변경은 이전 이름을 즉시 다시
  확보할 수 있다는 보장이 없다. V2 dry-run과 프로필 자산 검수를 마친
  뒤 실제 계정을 변경한다.
- **코드 롤백:** V1 경로와 데이터 파일을 V2 안정화 전까지 보존하고
  `PIPELINE_VERSION` 또는 동등한 명시적 전환 설정으로 한 버전만
  예약 발행한다. 문제가 생기면 V2 예약을 끄고 V1 워크플로로 되돌린다.
  V2 일일 원장은 삭제하지 않아 감사 기록으로 남긴다.
- **콘텐츠 롤백:** 잘못 발행된 게시물의 자동 삭제는 하지 않는다.
  운영자가 사실관계를 검토해 Instagram에서 수정·보관·삭제를 결정하고
  GitHub 원장에 조치 결과를 기록한다.
